<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!--mybatis的主配置文件-->
<configuration>
    <!--配置环境-->
    <environments default="mysql">
        <!--配置mysql的环境-->
        <environment id="mysql">
            <!--配置事务类型，指定为JDBC-->
            <transactionManager type="JDBC"></transactionManager>
            <!--配置数据源（连接池）-->
            <dataSource type="POOLED">
                <!--配置连接数据库的4个基本信息-->
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/eesy_mybatis"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>
    </environments>

    <!--指定映射配置文件的位置-->
    <mappers>
        <!--1、使用映射配置文件，指定每个dao在resources的独立的配置文件mapper.xml-->
        <!--<mapper resource="com\mori\demo\mapper\UserMapper.xml"/>-->
        <!--2、使用注解，指定dao接口的全类名。此时不用在resources创建mapper.xml了
            可以理解为mapper.xml是单独指定了namespace，这里是直接指定了namespace，
            所以两个namespace中的方法不能重名-->
        <mapper class="com.mori.demo.mapper.UserMapper"/>
    </mappers>
</configuration>